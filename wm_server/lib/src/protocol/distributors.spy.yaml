class: Distributors
table: distributors
fields:
    distributorName: String
    email: String
    userID: int, relation(parent=serverpod_user_info)
    joinDate: DateTime
    currentRankId: int, relation(parent=ranks)
    isActive: bool