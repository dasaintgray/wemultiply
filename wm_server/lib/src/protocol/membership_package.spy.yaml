class: MembershipPackage
table: membership_packages
fields:
    name: String  # e.g. x1, x2, x3, x5, crown, crown_diamond
    price: double
    bottlesIncluded: int
    cashback: double?
    description: String?
indexes:
    idx_membership_packages_name:
      fields: name
      unique: true
